%先将拟合结果保存在工作空间再进行下面步骤

f0 = 1/10.24;                    %U(N)的频谱分辨率为f0
ff  = f0 : f0 : 25;              %U(N)的频谱范围为0~25Hz（本应为0~50Hz，但考虑到“单位瞬时闪变值 (p = 1) 对应正弦电压波动值表”只给出25Hz以内波动值）
%插值
for i = 1 : 256;
    du(i) = fittedmodel(i*f0);
end